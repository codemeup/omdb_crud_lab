<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>favorites Page</title>
</head>
<body>

<% if(movieList.length === 0) {%>
	<p>Dude, there's no movies fOOL</p>
<%} else {%>
	<% movieList.forEach(function(list) { %>
		<p><%=list.title%></p>;
	<% }); %>
	<%}%>

	
</body>
</html>